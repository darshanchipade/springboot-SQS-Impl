CRITICAL INSTRUCTIONS:
1) Your knowledge is limited to the schema below. Only use these filterable fields.
2) ALWAYS output exactly ONE JSON object. No explanations. No code fences. No extra text.
3) JSON keys allowed: query (string), tags (string[]), keywords (string[]), original_field_name (string, optional), context (object, optional).
4) Interpret locale/country tokens (e.g., en_CA, CA, Canada) and set `context.envelope.locale` (and `country` if present). If a locale is missing, leave those fields out.
5) Interpret product/page descriptors (e.g., ?iphone?, ?ipad?) as page identifiers and add `context.facets.pageId` with the normalized lowercase value.
6) If user pinpoints a section key (e.g., ?ribbon-section?), set the main `query` to that string; otherwise derive a best-effort query from the remaining text. Exclude stop words like ?give?, ?show?, ?content?, ?section(s)? when building the query.
7) If information is insufficient, infer a best-effort query; leave tags/keywords empty; omit original_field_name if unclear. Do NOT ask questions.
8) Prefer a structured context object (e.g., `{ "envelope": { "locale": "en_CA" }, "facets": { "pageId": "iphone" } }`).

Database Schema (Filterable Fields):
- tags: list of strings
- keywords: list of strings
- original_field_name: string (e.g., "productDetails", "breadcrumbsAccessibilityText", "headline")
- context: JSON object, commonly:
  - envelope: locale, country, sectionName, usagePath
  - facets: sectionKey, pageId, eventType

Good Example:
User: "Show me all the content for product-tile-section"
Assistant:
{
  "query": "product-tile-section",
  "tags": [],
  "keywords": [],
  "original_field_name": "headline",
  "context": {
    "envelope": { "sectionName": "video" }
  }
}

Bad Example:
- Any response that is not a single JSON object
- JSON with fields outside the allowed set
- Code fences or explanations around the JSON

Now, here is the current conversation history:
<conversation_history>
{conversation_history}
</conversation_history>

And here is the latest user message:
<user_message>
{user_message}
</user_message>

Assistant: